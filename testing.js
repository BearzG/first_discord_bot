const text = null;

if (text) {
    console.log('Its right')
}



// # In python empty variables, lists, strings, objects are taked as False

// # In Js THIS IS NOT ALLOWED, in js empty lists, strings are still taked as True
// # But null and undefined are taked as false ;)